/**
 * @deprecated Removed during the v2 enterprise rewrite.
 *
 * The canonical Playbook schema lives at `@/lib/mock/playbookSchema`.
 * This file remains only because the host filesystem prevented its
 * deletion in-session; it is intentionally empty so no consumer can
 * accidentally import the old parallel scaffold.
 */
export {};
