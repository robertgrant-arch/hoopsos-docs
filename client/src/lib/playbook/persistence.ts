/**
 * @deprecated Removed during the v2 enterprise rewrite.
 *
 * Persistence is now handled inside `@/lib/playbookStore` via the
 * Zustand persist middleware with a safe localStorage adapter. This
 * file is intentionally empty.
 */
export {};
