/**
 * @deprecated Removed during the v2 enterprise rewrite.
 *
 * The active Playbook store lives at `@/lib/playbookStore`. This module
 * was an unused parallel scaffold; it is intentionally empty.
 */
export {};
